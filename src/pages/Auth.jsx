import{useState}from'react';import{useAuth}from'../contexts/AuthContext';import{Button,Field}from'../components/comic/Comic';
export default function Auth(){const a=useAuth(),[signup,setSignup]=useState(false),[v,setV]=useState({name:'',email:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await(signup?a.emailSignup(v.name,v.email,v.password):a.emailLogin(v.email,v.password))}catch(e){setError(e.message)}finally{setBusy(false)}};return <main className="auth-page"><section className="auth-art"><div className="auth-logo"><span>HISAB</span><b>KITAB!</b></div><div className="pow">₹</div><h1>MONEY.<br/><em>SPLIT CLEARLY.</em><br/>SPENT SMARTLY.</h1><p>Your personal and household money, without the mystery.</p></section><section className="auth-form panel"><span className="kicker red">OPEN THE BOOK</span><h2>{signup?'MAKE AN ACCOUNT':'WELCOME BACK!'}</h2><p>{signup?'Start a ledger that actually makes sense.':'Your money story is waiting.'}</p>{!a.configured&&<div className="demo-warning"><b>DEMO MODE</b><span>Firebase keys are placeholders. Explore locally, or configure Firebase for real accounts.</span></div>}<form onSubmit={submit}>{signup&&<Field label="YOUR NAME"><input required value={v.name} onChange={e=>setV({...v,name:e.target.value})}/></Field>}<Field label="EMAIL"><input type="email" required autoComplete="email" value={v.email} onChange={e=>setV({...v,email:e.target.value})}/></Field><Field label="PASSWORD"><input type="password" required minLength="6" autoComplete={signup?'new-password':'current-password'} value={v.password} onChange={e=>setV({...v,password:e.target.value})}/></Field>{error&&<p className="form-error">{error}</p>}{a.configured?<><Button disabled={busy}>{busy?'ONE SEC…':signup?'SIGN ME UP!':'LET ME IN!'}</Button><button type="button" className="google" onClick={a.googleLogin}>G&nbsp; CONTINUE WITH GOOGLE</button></>:<Button type="button" onClick={a.loginDemo}>ENTER THE DEMO →</Button>}</form>{a.configured&&<button className="auth-switch" onClick={()=>setSignup(!signup)}>{signup?'Already have an account? Log in':'New here? Create an account'}</button>}</section></main>}
