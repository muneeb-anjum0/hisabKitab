import{useEffect,useState}from'react';import{Navigate,Route,Routes}from'react-router-dom';import{useAuth}from'./contexts/AuthContext';import{useData}from'./contexts/DataContext';import Nav from'./components/navigation/Nav';import QuickAdd from'./components/forms/QuickAdd';import Dashboard from'./pages/Dashboard';import Funds from'./pages/Funds';import FundDetail from'./pages/FundDetail';import Activity from'./pages/Activity';import Summary from'./pages/Summary';import Profile from'./pages/Profile';import Auth from'./pages/Auth';
export default function App(){const{user,loading}=useAuth(),data=useData(),[quick,setQuick]=useState(false),[online,setOnline]=useState(navigator.onLine);useEffect(()=>{const on=()=>setOnline(navigator.onLine);addEventListener('online',on);addEventListener('offline',on);return()=>{removeEventListener('online',on);removeEventListener('offline',on)}},[]);if(loading)return <div className="splash"><div className="mini-logo">HK!</div><span>OPENING THE BOOK…</span></div>;if(!user)return <Auth/>;return <div className="app-shell"><Nav onQuick={()=>setQuick(true)}/><main className="content">{!online&&<div className="offline">OFFLINE — SAVED FIRESTORE WRITES WILL SYNC WHEN YOU RETURN.</div>}{data.error&&<div className="error-banner">{data.error}<button onClick={data.refresh}>RETRY</button></div>}<Routes><Route path="/" element={<Dashboard/>}/><Route path="/funds" element={<Funds/>}/><Route path="/funds/:id" element={<FundDetail/>}/><Route path="/activity" element={<Activity/>}/><Route path="/summary" element={<Summary/>}/><Route path="/profile" element={<Profile/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes></main>{quick&&<QuickAdd onClose={()=>setQuick(false)}/>}</div>}
