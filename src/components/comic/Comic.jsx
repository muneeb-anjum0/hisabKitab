export function Panel({children,className='',as:Tag='section',...p}){return <Tag className={`panel ${className}`} {...p}>{children}</Tag>};export function Button({children,variant='ink',className='',...p}){return <button className={`ink-button ${variant} ${className}`} {...p}>{children}</button>};export function Burst({children,className=''}){return <span className={`burst ${className}`}>{children}</span>};export function Empty({title='THIS PLACE IS TOO QUIET...',children}){return <div className="empty"><div className="empty-burst">?</div><h3>{title}</h3><p>{children}</p></div>}
export function Field({label,error,children,className=''}){return <label className={`field ${className}`}><span>{label}</span>{children}{error&&<small className="field-error">{error}</small>}</label>}
export function Modal({title,onClose,children,wide=false}){return <div className="modal-backdrop" role="presentation" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className={`modal panel ${wide?'wide':''}`} role="dialog" aria-modal="true" aria-labelledby="modal-title"><button className="modal-close" onClick={onClose} aria-label="Close">×</button><div className="torn-label">DO THE MATH</div><h2 id="modal-title">{title}</h2>{children}</section></div>}
export function Progress({value,max,label}){const p=max?Math.min(100,Math.max(0,value/max*100)):0;return <div className="progress-wrap"><div className="progress-label"><span>{label}</span><b>{Math.round(p)}%</b></div><div className="progress"><i style={{width:`${p}%`}}/></div></div>}
